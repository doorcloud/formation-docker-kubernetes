apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: pod-deleter
  labels:
    app.kubernetes.io/name: pod-deleter
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["delete"]
