apiVersion: v1
kind: Pod
metadata:
  name: crash-loop
  labels:
    app.kubernetes.io/name: crash-loop
spec:
  restartPolicy: Always
  securityContext:
    runAsNonRoot: true
    runAsUser: 65534
    runAsGroup: 65534
    seccompProfile:
      type: RuntimeDefault
  containers:
    - name: crash
      image: ghcr.io/doorcloud/formation/busybox:1.36
      imagePullPolicy: IfNotPresent
      command: ["sh", "-c", "echo boot; exit 1"]
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop: ["ALL"]
        readOnlyRootFilesystem: true
      resources:
        requests:
          cpu: 10m
          memory: 16Mi
        limits:
          cpu: 50m
          memory: 32Mi
